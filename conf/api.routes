# Routes For API
# ~~~~

GET     /                                                   controllers.api.HomeController.index
GET     /search/:kw/:start/:rows/:sort/:fq                  controllers.api.HomeController.generalSearch(kw, start, rows, sort, fq)

